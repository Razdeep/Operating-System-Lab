/**
 * Write a program that has process hierarchy p1 -> p2 where p1 and p2
 * have separate threads t1 and t2. 
 * Now P1 is creating a file and user would be enterring data in the 
 * file. P2 is opening the same created file and reading the first 5
 * characters from the file and displaying ont the screen
 * 
 */
#include <stdio.h>
int main(int argc, const char** argv)
{
    
    return 0;
}